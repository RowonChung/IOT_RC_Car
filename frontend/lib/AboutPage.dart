import 'package:flutter/material.dart';


class AboutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('회원 상세페이지'),
      ),
      body: Center(
        child: Text('컨트롤 페이지입니다.'),
      ),
    );
  }
}